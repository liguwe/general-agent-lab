查看当前所有 git 变更（git status 和 git diff），将所有修改暂存（git add -A），
然后根据变更内容生成一个简洁、规范的 commit message 并提交。

Commit message 规范：
- 使用 conventional commits 格式（feat/fix/refactor/docs/chore 等）
- 第一行不超过 72 字符
- 如有必要，空一行后加详细描述

直接执行，不需要确认。
