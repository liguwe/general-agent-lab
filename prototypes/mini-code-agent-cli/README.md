# mini-code-agent-cli

这个目录用于沉淀最小 Code Agent CLI 汇总骨架。

它服务于 `general-agent-lab` 主线：CLI 是入口，最终目的是把可复用模式沉淀成对 `General Agent Product` 的判断。

每日原型先在 `prototypes/NNN-<topic>/` 下独立跑通，编号从 `001` 递增。只有经过验证、确实可复用的部分，才吸收到这里。

优先沉淀 Agent Loop、工具调用、权限确认和执行反馈。实现时保持最小可运行，不引入和当前验证目标无关的框架或抽象。
