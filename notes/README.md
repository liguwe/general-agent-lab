# notes

源码阅读、对照和模式提炼笔记，平铺在 `notes/` 下即可，不再分子目录。

命名由你自己定，例如：

```text
0. codex-cli 技术栈分析.md
1. opencode agent loop 对照.md
```

优先围绕 Codex CLI 和 opencode：先记真实代码路径和调用链，再写对 mini code agent CLI 的启发。插图放在仓库根目录的 `assets/`，在笔记里用相对路径引用，例如 `../assets/codex-cli-stack.png`。
